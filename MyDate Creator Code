//This part of the program produces two 
//MyDate objects and prints out the two
//dates in the console.

public class Main {
	/** Main method (Every program has one) */
	public static void main(String[] args) {
		// Create two MyDate objects
		MyDate date1 = new MyDate();
		MyDate date2 = new MyDate(34355555133101L);

		// This prints out the two dates in the console
		System.out.println("Date1: " + date1.getMonth() + "/" + date1.getDay() + 
			"/" + date1.getYear());
		System.out.println("Date2: " + date2.getMonth() + "/" + date2.getDay() + 
			"/" + date2.getYear());
	}
}
